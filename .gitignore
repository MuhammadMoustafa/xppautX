# personal scratch models, never committed
sandbox/

# what a run writes next to the model; examples_check compares it and a
# committed one was being copied into every run directory
output.dat

*.o
*.a
*.so
*.dll
*.exe
/xppaut
*~
._*
.DS_Store
/v8/
*.tgz
*.bak
*conflicted copy*
# everything a build writes; build/legacy holds upstream Makefiles, kept
/build/*
!/build/legacy/
/xppautX
/libxppcore.a
/xppautX-*

# Python bytecode from the check scripts
__pycache__/
